# 贡献说明

欢迎提交问题、案例、测试和改进建议。

## 提交前

- 保持 Skill 只在明确调用 `sherry-skillforhtml2026` 时触发。
- 不要把个人路径、真实业务资料、密钥或生成产物提交到仓库。
- 修改流程、门禁或产物规则时，同时补充对应测试。
- 本地运行 `npm test`、`npm run test:python` 和 `npm run check:open-source`。

## Pull Request

请说明问题、改动范围、验证方式，以及是否影响 HTML、PPTX、讲稿或逐阶段确认流程。保持单个 PR 聚焦一个主题。
